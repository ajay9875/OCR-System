# Change Execution Policy using below command on powerShell
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned

#To create a virtualenvironment
python -m venv venv 

#To activate Virtual environment
.\venv\Scripts\activate

#To install flask in virtual environment
pip install flask

#To import requsts
import requests(in app.py)
pip install requests(command)

#To install flask login
pip install flask login





--------To create Database------------------
#To create Database in virtual environment
pip install flask-sqlalchemy 

#After creating a schema in app.py successfully run app.py
python app.py


--------For Making A Recommendable Using Ai Button---------

#Ensure you have pandas Scikit-Learn installed:
pip install pandas scikit-learn

#Make a data folder and make courses.csv file within that folder
data\courses.csv

#Give the all details of the our courses such as
Course Name,Category,Description,Difficulty Level,Duration (weeks),Skills Covered

#Import these library or module in the app.py
import pandas as pd
from sklearn.feature_extraction.text import TfidfVectorizer
from sklearn.metrics.pairwise import cosine_similarity

# Install fuzzywuzzy for more related string matching(Makes recommendations better)
pip install fuzzywuzzy









--------To Deploy our app(website code) at Heroku-----------
# Download and Install git from chrome browser
# Check command below after git installation(Do this without opening virtual environment)
git init

#pip install gunicorn
# Open your virtual environment using below command after that use further command
.\venv\Scripts\activate
git config --global user.email "you@example.com"
pip freeze > requirements.txt
git add .
git commit -m '1st commit'

#Create a Procfile in the same folder where app.py is.
Write thin in Procfile:-> web:gunicorn app:app

#Write heroku in command prompt to check
heroku(if it is working then proceed further otherwise add path of heroku CLI in environment variable) 
heroku login (Please login on redirected page using herku login id as email and password)

#Install git repository through Chrome Browser
Install git

#command for git
git init
git add .
git commit -m "initial commit"

#Command for heroku
heroku create my-todo-website or name as you want



